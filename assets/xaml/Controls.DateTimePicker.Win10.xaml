<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                    xmlns:system="clr-namespace:System;assembly=mscorlib">

    <!--
        The square Windows 10 look for DateTimePicker and TimePicker: no
        rounding anywhere, a taller field than the library's default, the
        square Win10 calendar in the drop-down, and the same three-column time
        selection - Windows 10 had no clock face either.

        Like the WinUI file this is a style rather than a replacement template.

        Drop-in: merge Controls.Calendar.Win10.xaml and then this dictionary
        after Controls.xaml, and give the pickers an implicit style based on
        the two below.
    -->

    <CornerRadius x:Key="MahApps.CornerRadius.DateTimePicker.Win10">0</CornerRadius>
    <system:Double x:Key="MahApps.Sizes.DateTimePicker.Win10.MinHeight">32</system:Double>

    <Style x:Key="MahApps.Styles.ComboBoxItem.DateTimePicker.Win10" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="Padding" Value="12 6" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                        <ContentPresenter x:Name="ContentPresenter"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource MahApps.Brushes.Gray8}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource MahApps.Brushes.Accent}" />
                            <Setter TargetName="ContentPresenter" Property="TextElement.Foreground" Value="{DynamicResource MahApps.Brushes.IdealForeground}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value=".5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MahApps.Styles.ComboBox.DateTimePicker.Win10"
           BasedOn="{StaticResource MahApps.Styles.ComboBox}"
           TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource MahApps.Styles.ComboBoxItem.DateTimePicker.Win10}" />
        <Setter Property="MaxDropDownHeight" Value="240" />
        <Setter Property="MinHeight" Value="36" />
        <Setter Property="MinWidth" Value="48" />
        <Setter Property="Padding" Value="8 4" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.DateTimePicker.Win10}" />
        <!--  No chevron: the whole field is the toggle, and a column reads better without one.  -->
        <Setter Property="mah:TextBoxHelper.ButtonWidth" Value="0" />
    </Style>

    <Style x:Key="MahApps.Styles.Label.DateTimePicker.Win10" TargetType="{x:Type Label}">
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Text}" />
        <Setter Property="Opacity" Value=".4" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <Style x:Key="MahApps.Styles.DateTimePicker.Win10"
           BasedOn="{StaticResource {x:Type mah:DateTimePicker}}"
           TargetType="{x:Type mah:DateTimePicker}">
        <Style.Resources>
            <Style BasedOn="{StaticResource MahApps.Styles.ComboBox.DateTimePicker.Win10}" TargetType="{x:Type ComboBox}" />
            <Style BasedOn="{StaticResource MahApps.Styles.Label.DateTimePicker.Win10}" TargetType="{x:Type Label}" />
        </Style.Resources>
        <Setter Property="CalendarStyle" Value="{DynamicResource MahApps.Styles.Calendar.Win10}" />
        <Setter Property="IsClockVisible" Value="False" />
        <Setter Property="MinHeight" Value="{DynamicResource MahApps.Sizes.DateTimePicker.Win10.MinHeight}" />
        <Setter Property="Padding" Value="8 4" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.DateTimePicker.Win10}" />
    </Style>

    <Style x:Key="MahApps.Styles.TimePicker.Win10"
           BasedOn="{StaticResource {x:Type mah:TimePicker}}"
           TargetType="{x:Type mah:TimePicker}">
        <Style.Resources>
            <Style BasedOn="{StaticResource MahApps.Styles.ComboBox.DateTimePicker.Win10}" TargetType="{x:Type ComboBox}" />
            <Style BasedOn="{StaticResource MahApps.Styles.Label.DateTimePicker.Win10}" TargetType="{x:Type Label}" />
        </Style.Resources>
        <Setter Property="IsClockVisible" Value="False" />
        <Setter Property="MinHeight" Value="{DynamicResource MahApps.Sizes.DateTimePicker.Win10.MinHeight}" />
        <Setter Property="Padding" Value="8 4" />
        <Setter Property="mah:ControlsHelper.CornerRadius" Value="{DynamicResource MahApps.CornerRadius.DateTimePicker.Win10}" />
    </Style>

</ResourceDictionary>
